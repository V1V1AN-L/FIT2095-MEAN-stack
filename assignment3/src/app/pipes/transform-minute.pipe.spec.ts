import { TransformMinutePipe } from './transform-minute.pipe';

describe('TransformMinutePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformMinutePipe();
    expect(pipe).toBeTruthy();
  });
});
